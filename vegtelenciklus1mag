#include <unistd.h>
#include <stdio.h>
            int main()
            {
                    for(;;)
                    {
                    	printf("fut\n");
                        sleep(1);   /* Egy proceszor magon fut 100%-on a végtelen ciklus. 
                    }
                    return 0;
            }
